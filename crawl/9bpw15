{"url": "https://www.reddit.com/r/learnprogramming/comments/9bpw15/i_need_to_try_to_find_the_highest_score_out_of/", "text": "x = 30\nnames = [\"\"] * x\ntest1 = [-1] * x\ntest2 = [-1] * x\ntest3 = [-1] * x\ntotal = 0\ntemp = 0\n\n\nfor i in range(x):\nnames[i] = input(\"What is the name of the student, \")\n\nwhile test1[i] < 0 or test1[i] > 20:\n   test1[i] = int(input(\"What is the student's score for the first test\"))\n\nwhile test2[i] < 0 or test2[i] > 25:\n    test2[i] = int(input(\"What is the student's score for the second test\"))\n\nwhile test3[i] < 0 or test3[i] > 35:\n    test3[i] = int(input(\"What is the student's score for the third test\"))\n\ntotal = test1[i] + test2[i] + test3[i]\n\nprint(names[i],\"'s total score is\", total)\n\ntemp = test1[i] + test2[i] + test3[i] + temp\n\naverage = temp/x\n\nprint(average)\n\nI have all the indents dont worry, it's just hard formatting in reddit.\n\nThis is my current code and I already found out the average which was a previous question. I now need to figure out how to get the highest score from the array and output it. Please put it as simply as possible so I can understand it, thanks a lot :)", "score": 1, "comments": [{"body": "You need to put 4 spaces in front of each line of code to format it as a code block. There are websites and nice Chrome extensions to do this automatically just by a highlight and right click. [CTG Plugins](https://chrome.google.com/webstore/detail/ctg-plugins/hjljaklopfcidbbglpbehlgmelokabcp) is a toolbox Chrome extension I made which has this feature as I described.\n\nFinding the max is pretty simple. The basic idea is to keep track of the current max in a variable and compare each student in the list against the current max. If the current student is higher than the current max, then update the max.", "id": "e54vny7", "replies": [{"body": "Something like this right?\n\n    def get_highest_score(score_list):\n        temp_max = score_list[0] # setting a temporary max number\n        for score in score_list[1:]:\n            # if the current score is greater than the temp_max, set it as the new temp_max\n            if score > temp_max:\n                temp_max= score\n        return temp_max\n        \n\n&#x200B;", "id": "e556dax", "replies": [{"body": "Yup! This would be correct assuming the list is always guaranteed to have at least one element.", "id": "e55cjyi", "replies": []}]}, {"body": "Ah thats helpful thanks, great idea", "id": "e54wp4b", "replies": []}]}], "title": "I need to try to find the highest score out of all the students and output it"}