{"url": "https://www.reddit.com/r/learnprogramming/comments/9cfpfw/trigger_mousewheel_updown_by_swiping/", "text": "I am trying to make it possible for mobile users on my website to send mousewheel trigger since I have a function that only runs on mousewheel trigger.So what I am trying to do is making swipe left \"simulate\" a mousewheel up trigger and swipe right to simulate a mousewheel down trigger but I couldn't find anything like that for mousewheel triggers...\n\nhere is my swipe event by the way:\n\nfunction handleTouchMove(evt) { if ( ! xDown || ! yDown ) { return; }\n\n        var xUp = evt.touches[0].clientX;\n        var yUp = evt.touches[0].clientY;\n    \n        var xDiff = xDown - xUp;\n        var yDiff = yDown - yUp;\n    \n        if ( Math.abs( xDiff ) > Math.abs( yDiff ) ) {/*most significant*/\n            if ( xDiff > 0 ) {\n                /* left swipe */\n            } else {\n                /* right swipe */\n            }\n        } else {\n            if ( yDiff > 0 ) {\n                /* up swipe */\n            } else {\n                /* down swipe */\n            }\n        }\n        /* reset values */\n        xDown = null;\n        yDown = null;\n    };\n\nbasicly I am looking to put under the `/* up swipe */` and the `/* down swipe */` the mousewheel trigger.  \n\n\nEDIT: forgot to say JS in the title, sorry...", "score": 0, "comments": [{"body": "Maybe post your question again or on a different subreddit?\n", "id": "e5b9qzl", "replies": [{"body": "I will try asking on r/javascript . thanks :)", "id": "e5bll7s", "replies": []}]}], "title": "Trigger mousewheel up/down by swiping"}