{"url": "https://www.reddit.com/r/learnprogramming/comments/9cmy4v/number_of_characters_in_a_line/", "text": "Hey Folks, \n\nI have a homework assignment to read text from a file, print the words, and at the end of the word print the number of characters in that word. \n\nThe main method prints total lines read, while the second method does the rest. I'm **ALMOST** there my output is just counting the letters and adding the number of letters from the word below. Code is below.\n\n    import java.io.File;\n    import java.io.FileNotFoundException;\n    import java.util.Scanner;\n\n    public class CountWords {\n    public static void main(String[] args){\n        int lineCount;\n        lineCount = processFile(\"words.txt\");\n\n        System.out.printf(\"\\n%d lines were processed\\n\", lineCount);\n        System.out.println(\"Done!\");\n    }\n\n    public static int processFile(String fileName){\n        int n = 0;\n        int lineCount = 0;\n        int charCount = 0;\n        try {\n            Scanner input = new Scanner(new File(fileName));\n            while(input.hasNextLine()){\n            String line = input.nextLine();\n           {for (int i = 0; i < line.length(); i++ ){\n               charCount++;\n           }\n            }\n            System.out.println(n + \".  \" +line + \"(\" + charCount + \")\" );\n            n++;\n            lineCount++;\n\n            }\n            input.close();\n        } catch (FileNotFoundException e) {\n            System.err.println(\"Ouch.  Something went wrong!!\");\n            System.err.println(e.getMessage());\n            System.exit(1);\n\n        }\n\n        return lineCount;\n    }\n    } ", "score": 2, "comments": [{"body": "Looks like you are reading line and counting letters for the whole line.  If I understand the assignment correctly you might want to think about how you can iterate through each word in a given line as well.", "id": "e5bsvcz", "replies": []}, {"body": "You don't reset `charCount` between lines, but that variable is useless anyway because you can simply print `line.length()`.", "id": "e5bssv3", "replies": [{"body": "Thank you. That got me the right output. I removed the charCount and references to it. Does everything else look good?", "id": "e5bt6ky", "replies": []}]}], "title": "Number of Characters in a line"}