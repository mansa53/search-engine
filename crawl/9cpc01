{"url": "https://www.reddit.com/r/learnprogramming/comments/9cpc01/c_trying_to_find_the_root_of_a_number_using_the/", "text": "    #include <stdio.h>\n    #include <math.h>\n    #include <complex.h>\n    #define disc (b*b -(4*a*c))\n    int main() {\n\n    printf(\"Please enter the values for a, b, and c where a > 0 \\n\");\n\n    int a, b, c;\n    scanf(\"%d %d %d\", &a, &b, &c);\n    double quadepos; //quadepos = Quadratic equation positive\n    double quadeneg; //quadeneg = Quadratic equation negative\n    quadepos = (-b + sqrtf(b*b - (4*a*c)))/(2*a);\n    quadeneg = (-b - sqrtf(b*b - (4*a*c)))/(2*a); \n\n    if (disc > 0)\n        printf(\"There real roots at: %.2lf, %.2lf\", quadepos, quadeneg);\n\n    else if (disc < 0)\n        printf(\"There are complex roots at: %.2lf + %.2lfi, %.2lf - %.2lfi\", quadepos,quadeneg, quadepos, quadeneg);\n\n    else if (disc == 0)\n        printf(\"There is a repeated root at: %.2lf\",quadepos);\n\n    else if (a == 0)\n        printf(\"coefficient a cannot be 0\");\n    return 0;\n    }\n\nI am having trouble with the first else if statement. Whenever I input three positive values, for instance 12, 10, and 14, I get:\n\n    There are complex roots at: -nan(ind) + -nan(ind)i, -nan(ind) - -nan(ind)i\n\nIs there a way I can display the imaginary numbers in the answer? I am just starting out so any additional information will also be helpful!", "score": 2, "comments": [{"body": "If a = 12, b = 10 and c = 14 then `b*b-4*a*c` is -572. \n\n`sqrt` returns `NaN` for negative numbers.", "id": "e5caz9l", "replies": [{"body": "Thank you for the reply. Do you know if there is a library that can output imaginary numbers?", "id": "e5cb3m7", "replies": [{"body": "[Use `_Complex` from <complex.h>](https://stackoverflow.com/a/9860772/3496382)", "id": "e5cbbki", "replies": []}]}]}], "title": "[C] Trying to find the root of a number using the quadratic equation returns -nan(ind)"}